export const initialState = {
  heroPosX: 110,
  heroPosY: 100,
  staticRectangles: [
    //top, left, width, height
    [500, 100, 300, 1],
    [500, 500, 300, 1],
    [500, 900, 300, 1],
  ],
  landed: false, //is the hero on solid ground
};
