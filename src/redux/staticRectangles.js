import { initialState } from "./initialState";

export function staticRectangles(
  state = initialState.staticRectangles,
  action
) {
  switch (action.type) {
    default:
      return state;
  }
}
